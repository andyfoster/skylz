<!DOCTYPE html>
<html>
  <head>
    <title>Skylz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path('favicon.ico') %>    
  </head>

  <body class="dark:bg-slate-300">
    <%= render 'layouts/navbar' %>
    <main class="container mx-auto mt-5 px-5 flex">
      
      <%= yield %>
    </main>
  </body>
  <script>
  // let stepsBox = document.querySelector('#stepsTextarea');

  function setHeight(fieldId){
    document.getElementById(fieldId).style.height = document.getElementById(fieldId).scrollHeight+ 3 + 'px';
  }

  // setHeight("stepsTextarea");
  // setHeight("notesTextarea");
  

  </script>
</html>
