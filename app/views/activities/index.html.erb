<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Activities for <%= @domain.name %></h1>
    <!-- TODO: Add modal popup to add an activity for any skill -->
  </div>

  <div id="activities-table" class="min-w-full">
        <table class=" border-black">
          <tr>
            <th>Info</th>
            <th>Skill</th>
            <th></th>
            <th>Notes</th>
          </tr>
          <% @activities.each do |activity| %>
            <tr class="border-t border-b border-black">
              <td class="text-xs text-gray-500 italic">
                <%= activity.date %><br><%= activity.activity_type %>
                <br>
                <%= link_to "edit", edit_skill_activity_path(activity.skill, activity), class: "hover:font-bold" %>
              </td>
              <td><%= link_to activity.skill.name, skill_path(activity.skill), class: "text-purple-500" %></td>
              <td><%= activity.reps %>x</td>
              <td><%= activity.description %></td>
            </tr>
          <% end %> 
        </table>
  </div>

  <div>
    <% @activities.each do |activity| %>
    <div class="border border-2 border-indigo-900 mb-3 p-5">
    <div class="w-full">
      <span class="w-1/3 text-xl mb-3 pr-5 font-bold inline-block"><%= activity.skill.name %></span>
      <span class="w-1/3 text-xl mb-3 inline-block"><%= activity.description %></span>
    </div>
      <div class="text-xs mt-3">
        <em><%= activity.date.strftime("%a, %e %b %Y") %> -
          <span class="text-md leading-3 text-orange-900 inline">
            <%= activity.activity_type %>
          </span>
          <span class="text-indigo-900 ml-5"><%= activity.reps %>x</span>
        </em>
        <%= link_to "edit", edit_skill_activity_path(activity.skill, activity), class: "float-right hover:font-bold" %>
      </div>
    </div>
    <% end %>
  </div>
  <br>


  <div id="activities" class="min-w-full">
    <% @activities.each do |a| %>

      <div class="my-3">
        <%= link_to a.skill.name, skill_path(a.skill), class: "underline text-purple-500" %>
        <div class="">
          <div class="inline"><%= a.description %></div>
          <div class="inline text-gray-500 italic"><%= a.date %> - <%= a.reps %>x - <%= a.activity_type %></div>
        </div>
      </div>

    <% end %>
  </div>
</div>
