<%= form_with(model: domain, class: "contents") do |form| %>
  <% if domain.errors.any? %>
    <div id="error_explanation" class="px-3 py-2 mt-3 font-medium text-red-500 rounded-lg bg-red-50">
      <h2><%= pluralize(domain.errors.count, "error") %> prohibited this domain from being saved:</h2>

      <ul>
        <% domain.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5 mb-5">
    <%= form.label :name, class: "inline-block mr-5" %>
    <%= form.text_field :name, placeholder: "Karate", class: "shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 mr-5 w-2/3" %>
    <%= form.submit "Save", class: "btn-primary" %>
  </div>

  <div class="inline">
  </div>
<% end %>
